# 設計ドキュメント（運動でモンスターを倒すダイエットゲーム）

このドキュメントは、企画書（README.md）を実装に落とすための設計をまとめたものです。
「何を・なぜそう決めたか」を記録し、上流工程（要件・設計）の成果物として残します。

対象範囲：換算ロジック／セーブデータ構造／MVPのIssue分解

---

## 1. 運動→ダメージ換算ロジック

### 1.1 設計思想

攻撃力は「運動の**きつさ**」×「運動の**量**」で決める。
同じ「たくさんやった」でも、腕立て50回とウォーキング50歩では価値が違うため、
きつさ（単位あたりの重み）と量（どれだけやったか）を掛け合わせて差を表現する。

### 1.2 攻撃力を構成する3要素

1. **運動係数（きつさの重み）**：運動の種類ごとに持つ固定値。負荷が高いほど大きい。各運動の価値を決める"換算レート"。
2. **量（自己申告の入力値）**：ユーザーが記録する回数・距離・時間。運動ごとに単位が違う。
3. **基準値（全体スケールの調整）**：ゲーム全体のダメージを一括調整する係数。モンスターHPとのバランスを取るツマミ。

### 1.3 基本式

```
攻撃力 = 基準値 × 運動係数 × 量
```

### 1.4 数値設定

**基準値**

```
基準値 = 1
```

MVP段階では全体スケールを動かす必要がないため1に固定。後で一括調整したくなった時にこの値だけ動かす。

**運動係数（1単位あたりのダメージ）**

| 運動 | 単位 | 運動係数 | 「1回分」の目安ダメージ |
|---|---|---|---|
| 腕立て伏せ | 1回 | 2 | 10回 → 20 |
| スクワット | 1回 | 2 | 15回 → 30 |
| その場ジャンプ | 10秒 | 5 | 30秒 → 15 |
| プランク | 10秒 | 8 | 30秒 → 24 |
| ウォーキング | 1km | 15 | 2km → 30 |
| ランニング | 1km | 40 | 2km → 80 |

**計算例**

```
腕立て10回     = 1 × 2  × 10 = 20
ランニング2km  = 1 × 40 × 2  = 80
プランク30秒   = 1 × 8  × 3  = 24   （30秒 = 3単位）
```

**モンスターHPの目安**

```
最初のモンスターHP = 100
```

腕立て10回（=20）を5セット、またはランニング2km + スクワット15回（=110）あたりで1体討伐。
1回の記録では倒しきれず、数回の積み重ねで倒せる手応えを狙う。

### 1.5 設計判断の記録

- 腕立て・スクワットを係数2の基準に置き、他はそこからの相対で決めた（比較しやすくするため）。
- 有酸素運動は係数を高くしたが、2kmで80（HP100の8割）に収まるよう抑え、筋トレが割に合わなくならないよう調整。
- 秒単位の運動は「10秒 = 1単位」とし、数字が大きくなりすぎるのと入力の手間を防いだ。
- これらは初期値であり、検証で調整する前提。

### 1.6 今回は決めないこと

- 盛り対策（1日の上限、非現実値の扱い）── 後回し。
- MVP本体は「固定ダメージ」で割り切り、第2段階でこの式に差し替える。

---

## 2. セーブデータ構造

### 2.1 全体構造

localStorageには「全セーブスロットをまとめた1つのオブジェクト」を保存する。
スロットごとに別キーにするより、1キーにまとめる方がスロット一覧画面を作りやすい。

```json
{
  "version": 1,
  "slots": [
    {
      "slotId": 1,
      "playerName": "たろう",
      "level": 3,
      "attackPower": 12,
      "defeatedCount": 7,
      "currentMonster": {
        "name": "スライム",
        "maxHp": 100,
        "currentHp": 40
      },
      "history": [
        {
          "date": "2026-08-11T09:30:00",
          "exercise": "腕立て伏せ",
          "amount": 10,
          "damage": 20
        }
      ],
      "createdAt": "2026-08-01T08:00:00",
      "updatedAt": "2026-08-11T09:30:00"
    }
  ]
}
```

### 2.2 各フィールドの意味と設計理由

| フィールド | 型 | 意味・設計理由 |
|---|---|---|
| `version` | 数値 | データ構造のバージョン。将来の形式変換の判断に使う。 |
| `slots` | 配列 | 全セーブスロットの入れ物。要素数＝スロット数。 |
| `slotId` | 数値 | スロットの識別子。 |
| `playerName` | 文字列 | プレイヤー名。家族共有想定のため必須。 |
| `level` | 数値 | プレイヤーレベル。討伐数から算出。 |
| `attackPower` | 数値 | 現在の攻撃力。レベルボーナス等を反映する余地。 |
| `defeatedCount` | 数値 | 討伐した累計数。レベル計算とやり込み実感に使う。 |
| `currentMonster` | オブジェクト | 戦闘中のモンスター。HP途中経過を保存し「数日かけて倒す」を成立させる。 |
| `history` | 配列 | 運動記録の履歴。直近50件まで。 |
| `createdAt` / `updatedAt` | 文字列 | 作成・最終更新日時。スロット一覧の目印。 |

### 2.3 履歴の上限ルール（直近50件）

新しい記録は末尾に追加し、51件目を追加する時に先頭（最古）を削除する。

```js
history.push(新しい記録);
if (history.length > 50) {
  history.shift();  // 最古の1件を削除
}
```

**理由**：全件保存はlocalStorageの容量（概ね5MB）を長期的に圧迫しうる。
直近50件あれば傾向は振り返れる。累計実績は `defeatedCount` が別途持つため、履歴を削っても積み重ねは失われない。

### 2.4 レベルと討伐数の関係

```
レベル = floor(討伐数 / 3) + 1
```

| 討伐数 | レベル |
|---|---|
| 0〜2 | 1 |
| 3〜5 | 2 |
| 6〜8 | 3 |
| 9〜11 | 4 |

**理由**：

- 「3体倒すごとに1レベル」で序盤こまめに上がる刻みにし、達成感を早く頻繁に返す。
- レベルを討伐数から計算で導くことで、両者の食い違い（バグ）を防ぐ。
- 表示用に `level` も保存し、毎回計算し直さずに済むようにする。討伐時に討伐数を増やし、あわせて再計算・保存する。
- レベルアップ時の攻撃力ボーナス等は `attackPower` で受ける余地を残す（数値は第3段階）。

---

## 3. MVP Issue分解

### 3.1 依存関係

```
#1 データ構造の土台
      ↓
#2 セーブスロット画面 ── #3 運動記録＆ダメージ ── #4 討伐＆モンスター交代
                                    ↓
                            #5 運動メニュー提案
                                    ↓
                            #6 通し動作テスト
```

上から順に着手。#5は独立性が高く、先に進めてもよい。

### 3.2 各Issue

**#1 セーブデータの読み書き基盤をつくる**
- 内容：localStorageへの保存・読み込みの共通処理。設計した構造の初期化・取得・更新。
- 完了条件：空データ初期化ができる／スロットの取得・更新・保存ができる／再読み込みでデータが残る。
- 担当目安：Bさん（実装）　依存：なし

**#2 セーブスロット選択画面をつくる**
- 内容：起動時に「新規作成／続きから」を選ぶ画面。
- 完了条件：全スロットが一覧表示される（名前・レベル・最終プレイ日時）／新規作成で名前入力しスロットが増える／選ぶとゲーム画面に入る。
- 担当目安：Bさん（UI）　依存：#1

**#3 運動を記録してダメージを与える**
- 内容：MVPは運動1種・固定ダメージ。記録するとHPが減りHPバーに反映。履歴にも追加（50件ルール）。
- 完了条件：記録ボタンで固定ダメージが入る／HPバーが減る／履歴に追加され51件目で最古が消える／攻撃演出が出る。
- 担当目安：Bさん（戦闘・演出）　依存：#1
- 補足：MVPは固定ダメージで割り切り、第2段階で `基準値 × 運動係数 × 量` に差し替える。

**#4 討伐と次モンスターの出現**
- 内容：HP0で「討伐！」→ 討伐数+1 → レベル再計算 → 次モンスター出現。
- 完了条件：討伐演出が出る／`defeatedCount` が増える／レベルが再計算・保存される／次モンスターがHP満タンで出る。
- 担当目安：Bさん（戦闘）　依存：#3

**#5 運動メニューをランダム提案**
- 内容：ボタンでメニューリストから1つをランダム提案（第1段階まで）。
- 完了条件：用意したリストから1つ表示／押すたびに別のメニューが出る。
- 担当目安：Bさん（実装）／リストはAさん用意　依存：なし

**#6 MVP通し動作テスト**
- 内容：新規作成 → 記録 → 討伐 → 別スロット切替 → 再起動してデータ保持、を通しで確認。
- 完了条件：フローが最後まで通る／スロットを切り替えてもデータが混ざらない／再起動後もHP途中経過・討伐数・履歴が残る／不具合をIssue起票。
- 担当目安：Aさん（テスト・Issue管理）　依存：#1〜#5

### 3.3 Aさんが並行して用意しておくもの

- 運動メニューリスト（#5用）：設計済みの6種をベースに提案文言を整える。
- MVPの固定ダメージ値（#3用）：例「1記録=20ダメージ、モンスターHP=100」。

---

*このドキュメントは設計の出発点です。検証を通じて随時更新します。*

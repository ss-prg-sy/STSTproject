# 開発記録（運動でモンスターを倒すダイエットゲーム）

このドキュメントは、企画から実装までに「何を・なぜ・どう判断してやったか」の記録です。
就活での説明材料、および共同制作者との認識合わせを目的にまとめています。

- プロジェクト：運動を記録するとモンスターに攻撃できるダイエット継続ゲーム
- リポジトリ：github.com/ss-prg-sy/STSTproject
- 技術構成：HTML / CSS / JavaScript、データ保存は localStorage、バージョン管理は GitHub
- 体制：Aさん（課題定義・要件・設計・ドキュメント・テスト・Issue管理）／Bさん（実装・UI・演出・レビュー・環境構築）

---

## 1. 何を作ったか（成果物の全体像）

運動を記録すると、その内容に応じたダメージでモンスターを攻撃し、退治すると次の敵が現れる——という、
運動の成果を即座に可視化して継続を促すゲーム。ブラウザだけで動き、サーバー不要。

主な機能：
- 複数セーブスロット（家族での共有を想定）＋スロット単位の削除
- 6種の運動を選び、量を入力して記録（運動ごとにダメージが変わる換算式）
- 運動記録の履歴（直近50件、運動名・量・ダメージを表示）
- モンスターの HP を削り、退治すると討伐数が増えてレベルが上がる
- 退治するとオリジナル怪物が「おとしもの」を落とす
- 運動メニューのランダム提案
- 夕暮れの草原を舞台にした、オリジナルの怪物4体（SVGで描画）

---

## 2. どんな課題を解決しようとしたか

ダイエット・運動が続かない最大の原因は「成果が見えず実感がわかないこと」。
体重はすぐ変わらず、努力へのフィードバックが遅いため、多くの人が数日で挫折する。

そこで「運動 → 攻撃 → HP が減る」という因果をその場で見せ、努力の成果を即座に実感できるようにした。
加えて「何をすればいいか分からない」で始める前に止まる問題に対し、運動メニュー提案機能を用意した。

---

## 3. 設計上の判断（なぜそうしたか）

就活・実務で問われるのは「作った機能」よりも「なぜその選択をしたか」。ここに判断を残す。

### 3.1 まず MVP（最小実用製品）を作り、そこから育てる
検証したい仮説は「運動→ダメージの即時可視化が継続に効くか」。
これを確かめるのに必要な最小限（運動1種→固定ダメージ→討伐、複数セーブ、メニュー提案）だけを先に作った。
運動の種類・レベル制・RPG要素などは、仮説検証には今すぐ不要と判断し後回しにした。
→ 欲張らず、まず動いて検証できる形を最短で作るための判断。

### 3.2 運動量は自己申告制にした
歩数計やセンサー連携が理想だが、まず「仕組みが継続に効くか」を検証したい段階。
入力手段はシンプルにし、自己申告で割り切った。将来センサー連携へ拡張できる余地は残した。
→ 検証したいことに集中し、作り込みを後回しにする MVP の考え方に沿った判断。

### 3.3 ダメージ換算式を「3要素の掛け算」にした
攻撃力 = 基準値 × 運動係数 × 量。
役割を分けておくと、後から片方だけ調整できる（ゲームが簡単すぎる→基準値だけ、ランニングが強すぎる→運動係数だけ）。
全部を1つの数字に混ぜると、どこを直せばいいか分からなくなる。
→ 後からの調整しやすさ（保守性）を見据えた設計判断。

### 3.4 データはまず localStorage（ブラウザ内）に保存
サーバー・DB なしでも複数セーブは実現できる。環境構築でつまずかず、まず動くものを優先した。
「別端末からもログインしたい」等の要求が出た段階で、初めてサーバー・DB・ログインを検討する。
→ 必要になってから作る、という判断。

### 3.5 レベルは討伐数から計算で導く
レベル = floor(討伐数 / 3) + 1。
レベルを討伐数から計算することで、両者の食い違い（バグでレベルだけずれる）を防いだ。
表示用にレベルも保存し、毎回計算し直さずに済むようにした。
→ データの整合性を崩さないための設計判断。

### 3.6 作った機能をあえて削った（装備強化）
RPG の成長ループとして「おとしもの→そうび強化→攻撃力アップ」を一度実装したが、
遊んでみて MVP の検証には不要と判断し、装備強化を削除した（おとしものは討伐報酬として残した）。
→ 「足す」だけでなく「削る」判断も設計。設計は DESIGN / SPEC に残してあるので、必要なら復活できる。

---

## 4. 開発の進め方（どういう順で作ったか）

設計 → タスク分解（Issue）→ 実装 → テストで発見・修正、という流れを一周した。

1. 課題定義・要件定義（README）
2. 詳細設計（換算ロジック・セーブ構造・世界観・成長ループ）を DESIGN / SPEC に明文化
3. MVP を Issue に分解し、GitHub の Issue として起票
4. 実装（下記の順で段階的に機能を追加）
5. 動かして確認し、見つかったバグを修正

### 実装ステップ
1. MVP（運動記録・固定ダメージ・討伐・複数セーブ・メニュー提案）
2. 運動の種類を増やし、換算式でダメージを算出
3. 運動記録の履歴の見せ方を改善（運動名を主役に）
4. モンスターをオリジナルの怪物に差し替え（SVGで描画）
5. おとしもの（討伐で素材がたまる）
6. そうび強化（→のちに削除）
7. スロット単位のセーブ削除を追加
8. 全体のトーンを暗い森から明るい夕暮れの冒険に刷新

---

## 5. 実装中に見つけて直した不具合（テストの価値）

実際に動かして初めて分かった問題を、テストで見つけて直した。

- **同じ怪物が出続けるバグ**：怪物をレベルで選んでいたため、3体倒すまで同じ敵が出ていた。
  討伐数ベースで選ぶよう修正し、倒すたびに次の怪物へ変わるようにした。
- **強化しても攻撃力表示が更新されないバグ**：もちもの画面から戻る際に画面を再描画していなかった。
  戻る処理で再描画を呼ぶよう修正した。

→ 「設計どおり作った」だけでは動作は保証されない。動かして確かめる工程の大切さを実感した。

---

## 6. デザイン面の判断

- 怪物はオリジナルで制作（既存作品の固有名詞・デザインは使わない）。著作権に配慮した判断。
- 見た目は SVG（コードで描くベクター画像）で表現。画像ファイルを増やさず、色や形を後から調整でき、
  4体の絵柄を統一しやすいというメリットを取った。
- リアルさの追求には限界があるため（写実は AI 画像生成や 3D が必要）、SVG で出せる範囲の
  「立体的・獣寄り」に方向を定めた。道具の限界を理解したうえでの現実的な判断。
- 世界観は「ポップ・キャッチー × 拠点で迎え撃つ」。フィールド移動を作らず、実装をシンプルに保った。
- 全体トーンは、暗さで敬遠されないよう明るい夕暮れの冒険に。ただし怪物の怖さは残し、両立させた。

---

## 7. 役割分担

| 担当 | 主な内容 |
|---|---|
| Aさん | 課題定義／要件定義／換算ロジック設計／セーブ構造設計／世界観・成長ループ設計／ドキュメント／テスト／Issue管理 |
| Bさん | 戦闘処理／セーブUI／演出（HPバー・アニメーション）／コードレビュー／環境構築 |

上流工程（要件・設計・文書化）と実装・技術リードを分担し、それぞれの志望に沿った経験を残した。

---

## 8. 今後の展開（作ったあとにやること）

- GitHub へ反映（ブランチを切り、Pull Request 経由でマージ）
- 実機での通しテスト（新規作成→記録→討伐→スロット切替→再起動してデータ保持）
- 実際に自分たちや家族に使ってもらい、続いたか・どこで飽きたかを記録
- フィードバックを Issue に起こし、次の改善につなげる
- 将来の拡張候補：運動の種類追加、成長要素の復活、歩数計連携、サーバー・ログイン

---

## 9. 就活で話すときの要点（このプロジェクトから言えること）

- 課題（成果が見えず続かない）から解決方針（即時可視化）まで、筋を通して設計した
- 「まず最小限で検証し、必要になってから拡張する」という MVP の考え方を実践した
- 「なぜその選択をしたか」を都度言語化し、ドキュメントに残した（上流工程の経験）
- 作るだけでなく、遊んで確かめ、バグを見つけて直した（テストと改善のサイクル）
- 機能を「足す」判断だけでなく「削る」判断もした（目的に照らした取捨選択）
- 著作権や道具の限界を理解したうえで、現実的なデザイン判断をした
- チームで役割を分担し、Issue と Pull Request で開発を進めた

---

*この記録は制作の振り返りです。プロジェクトの進行に合わせて更新します。*
